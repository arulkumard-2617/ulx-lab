<template>
  <div class="p-6" data-qa="event-page-{{@model.section.id}}">
    <h1 class="h7-font">{{@model.section.label}}</h1>
    <p class="text-secondary mt-3">
      Add this page when you need it.
    </p>
  </div>
</template>
